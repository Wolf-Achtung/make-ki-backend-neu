# Backend requirements – Gold-Standard+ (Python 3.12)
# Fixes:
# - Pydantic EmailStr benötigt `email-validator`
# - Jinja2 für Templates (FastAPI/Starlette)
# - JWT-Handling mit python-jose
# - Uvicorn standard extras (uvloop, httptools, etc.)

fastapi==0.115.4
pydantic==2.8.2
pydantic-settings==2.4.0
email-validator==2.2.0
uvicorn[standard]==0.30.6
anyio==4.4.0
starlette==0.38.5
python-multipart==0.0.9
Jinja2==3.1.4

# Auth / Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# HTTP / Utilities
httpx==0.27.2
loguru==0.7.2

# Queue / Redis
redis==5.0.7
rq==1.16.1

# Optional: Leistungs-Boosts (werden mit uvicorn[standard] meist bereits installiert)
uvloop==0.20.0