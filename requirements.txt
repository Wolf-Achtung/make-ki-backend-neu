# Backend requirements (slim, uses external Node/Puppeteer PDF service)
# API & server
fastapi>=0.112,<1
uvicorn[standard]>=0.30,<1

# Config & templating
pyyaml>=6.0
jinja2>=3.1
python-dotenv>=1.0

# OpenAI SDK
openai>=1.0,<2

# IO & HTTP
aiofiles>=23.2
httpx>=0.27,<1

# Data / utils
pandas>=2.2,<3

# Auth / DB
psycopg2-binary>=2.9,<3
pyjwt>=2.8,<3
bcrypt>=4.0,<5
python-multipart>=0.0.9,<0.1

# Optional (uncomment only if actually used)
# requests>=2.31
# beautifulsoup4>=4.12
# markdown>=3.6
# matplotlib>=3.8
# google-search-results==2.4.2  # only if your SERP helper requires it
