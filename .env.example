# .env.example
#
# Sample environment configuration for the KI‑Status‑Report backend.  Copy this
# file to ``.env`` and adjust values according to your deployment needs.  Do
# not commit your actual ``.env`` to version control.

# Basic configuration
ENV=production
APP_VERSION=2025.10
LOG_LEVEL=INFO

# CORS configuration
# ``CORS_ALLOW_ORIGINS_RAW`` accepts JSON array, CSV or ``*``.  For example:
# CORS_ALLOW_ORIGINS_RAW=["https://ki-sicherheit.jetzt","https://make.ki-sicherheit.jetzt"]
# CORS_ALLOW_ORIGINS_RAW=https://ki-sicherheit.jetzt,https://make.ki-sicherheit.jetzt
CORS_ALLOW_ORIGINS_RAW=*

# Feature flags
# Set to ``true`` to enable background queue via Redis/RQ
QUEUE_ENABLED=false
# Provide a PDF service endpoint or disable it entirely
PDF_SERVICE_URL=
PDF_SERVICE_ENABLED=true

# Database connection (SQLAlchemy)
# Postgres connection string, e.g. ``postgresql://user:password@host:port/dbname``
DATABASE_URL=

# SMTP and email settings (if applicable)
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=
# SMTP_FROM_NAME=
