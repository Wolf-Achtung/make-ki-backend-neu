Produce a **risk matrix** (5 rows) with columns: Risk, Area, P, I, Score, Mitigation.
Context: consulting/SME; typical risks (prompt leakage, hallucinations, vendor lock-in, PII, quality).
**Output:** HTML fragment; a `<table>` with `<thead>` and `<tbody>` (semantic, compact).
